/**
 * i18n keys for the shared image-file schema, in this module's namespace.
 */
const IMAGE_FILE_MESSAGES = {
	size: "admin.trueFalse.validation.imageSize",
	type: "admin.trueFalse.validation.imageType",
} as const;

export { IMAGE_FILE_MESSAGES };
