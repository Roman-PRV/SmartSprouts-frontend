// A tap within this normalized distance of an existing marker toggles it off
// instead of dropping a second marker on top. Sized for imprecise child taps.
const MARKER_HIT_RADIUS = 0.04;

export { MARKER_HIT_RADIUS };
