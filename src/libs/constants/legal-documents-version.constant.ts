// Must match the backend's config/legal.php versions: the consent audit trail
// records what the user saw, so bump both together when the texts change.
const LEGAL_DOCUMENTS_VERSION = "2026-07-12";

export { LEGAL_DOCUMENTS_VERSION };
