/**
 * @type {import('prettier').Config}
 */

const config = {
	arrowParens: "always",
	bracketSpacing: true,
	printWidth: 80,
	quoteProps: "preserve",
	semi: true,
	singleQuote: false,
	useTabs: true,
};

export default config;
