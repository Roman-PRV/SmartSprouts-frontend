diff --git a/src/libs/components/navigation/navigation.tsx b/src/libs/components/navigation/navigation.tsx
index bf41a4b..55d2976 100644
--- a/src/libs/components/navigation/navigation.tsx
+++ b/src/libs/components/navigation/navigation.tsx
@@ -81,10 +81,28 @@ const Navigation: React.FC = () => {
 	const getNavLinkClassName = useCallback(({ isActive }: { isActive: boolean }): string => {
 		return getValidClassNames(
 			styles["navigation__nav-item"],
-			isActive && styles["navigation__nav-item--active"]
+			isActive &&
+				getValidClassNames(
+					styles["navigation__nav-item--highlighted"],
+					styles["navigation__nav-item--active"]
+				)
 		);
 	}, []);
 
+	const getGamesNavLinkClassName = useCallback(
+		({ isActive }: { isActive: boolean }): string => {
+			const isOnGamesTree = isActive || pathname.startsWith(`${AppRoute.GAMES}/`);
+			const isExactGames = pathname === AppRoute.GAMES;
+
+			return getValidClassNames(
+				styles["navigation__nav-item"],
+				isOnGamesTree && styles["navigation__nav-item--highlighted"],
+				isExactGames && styles["navigation__nav-item--active"]
+			);
+		},
+		[pathname]
+	);
+
 	return (
 		<nav className={getValidClassNames(styles["navigation"])}>
 			<div className={getValidClassNames(styles["navigation__container"])}>
@@ -110,7 +128,7 @@ const Navigation: React.FC = () => {
 						</NavLink>
 					</li>
 					<li>
-						<NavLink className={getNavLinkClassName} to={AppRoute.GAMES}>
+						<NavLink className={getGamesNavLinkClassName} end to={AppRoute.GAMES}>
 							{t("common.navigation.games")}
 						</NavLink>
 					</li>
diff --git a/src/libs/components/navigation/styles.module.css b/src/libs/components/navigation/styles.module.css
index f32e835..2310fa1 100644
--- a/src/libs/components/navigation/styles.module.css
+++ b/src/libs/components/navigation/styles.module.css
@@ -67,9 +67,12 @@
 	border-radius: var(--border-radius-sm);
 }
 
-.navigation__nav-item--active {
+.navigation__nav-item--highlighted {
 	color: var(--color-brand-hover);
 	font-weight: 700;
+}
+
+.navigation__nav-item--active {
 	pointer-events: none;
 	cursor: default;
 }
